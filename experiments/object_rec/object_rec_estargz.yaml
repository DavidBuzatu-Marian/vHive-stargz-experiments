apiVersion: serving.knative.dev/v1
kind: Pod
metadata:
  name: estargz-object_rec-test
spec:
  template:
    spec:
      containers:
        - name: object_rec-stargz
          image: ghcr.io/davidbuzatu-marian/object_rec:base
          ports:
            - containerPort: 8080
          imagePullPolicy: Always
