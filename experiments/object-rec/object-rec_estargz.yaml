apiVersion: serving.knative.dev/v1
kind: Pod
metadata:
  name: estargz-object-rec-test
spec:
  template:
    spec:
      containers:
        - name: object-rec-stargz
          image: ghcr.io/davidbuzatu-marian/object-rec:base
          ports:
            - containerPort: 8080
          imagePullPolicy: Always
