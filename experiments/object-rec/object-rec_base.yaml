apiVersion: serving.knative.dev/v1
kind: Pod
metadata:
  name: base-object-rec-test
spec:
  template:
    spec:
      containers:
        - name: object-rec-base
          image: ghcr.io/davidbuzatu-marian/object-rec:base
          ports:
            - containerPort: 8080
          imagePullPolicy: Always
